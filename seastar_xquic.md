Skill 文档：Seastar QUIC 多核收发优化与最终架构演进
目标
基于 Seastar 构建高性能 QUIC 通信系统，先修复当前 POSIX stack 下的发送路径稳定性问题，再优先评估 DPDK 用户态网络栈方案，最终形成适合高并发、弱网和高丢包场景的最优收发架构。
实现过程需分阶段推进，确保每一步都可独立验证、独立压测、独立回归。

背景与约束
当前 Seastar POSIX stack 下，非 shard-0 发送必须回到 shard-0，由统一出口发送，避免 unbound socket 和端口冲突问题。
现有系统已完成部分 shard 本地化优化，但接收侧仍可能存在单点收包与跨 shard 调度开销。
Seastar 同时支持 POSIX stack 和 DPDK stack，因此最终方案需要对两者进行评估，选择最优实现。
在未完成验证前，不应直接切换到复杂的内核级或 DPDK 改造方案。
Phase 1：修复发送路径，恢复稳定性
目标
保证所有非 shard-0 发送统一回到 shard-0，消除 Address already in use 和 unbound socket 问题。

要求
梳理所有发送入口
回退非 shard-0 发送到 submit_to(0, ...)
确保 shard-0 是唯一实际出包出口
增加日志和错误定位信息
验证
单连接可用
多连接可用
无端口冲突
无异常源端口
Phase 2：单元测试与基线压测
目标
验证回退后的发送路径正确性，并建立当前基线性能。

要求
补充功能测试
覆盖连接建立、收发、重连、异常恢复
在 smp=1 / 2 / 4 下进行压测
记录吞吐、延迟、CPU、丢包率
验证
功能回归通过
基线性能数据完整
确认当前瓶颈位置
Phase 3：优先评估 DPDK 路线
目标
先对比并验证 Seastar DPDK stack 是否能显著提升当前系统的收包与转发能力。

要求
调研 Seastar DPDK 集成方式
评估当前代码迁移成本
搭建 DPDK 原型环境
对比 POSIX stack 与 DPDK stack 的吞吐、延迟、CPU 占用、并发能力
分析是否满足部署条件与维护成本要求
验证
DPDK 路线可运行
可完成基础收发验证
可与 POSIX 基线做同条件对比
输出迁移收益评估
Phase 4：评估 POSIX + eBPF / RSS 路线
目标
在 DPDK 路线对比之后，再评估是否需要保留或补充 POSIX stack 下的内核级收包分发优化。

要求
分析 SO_REUSEPORT、eBPF、RSS、XDP 等机制
验证是否可在当前环境下缓解单点收包瓶颈
对比其性能收益、复杂度和运维成本
与 DPDK 路线做最终技术选型对比
验证
明确各方案收益
明确最终推荐架构
输出可执行的技术决策
Phase 5：实现最终最优架构
目标
根据前面两条路线的评估结果，落地最终方案。

可能结果
若 DPDK 明显优于 POSIX，则以 DPDK 为主实现最终版本
若 POSIX + eBPF/RSS 更适合当前环境，则保留 POSIX 并加强内核级分发
无论选择哪条路线，都要保留 shard 本地化处理和发送路径优化
验证
最终架构稳定可用
压测结果达到目标
可回归、可维护、可扩展
